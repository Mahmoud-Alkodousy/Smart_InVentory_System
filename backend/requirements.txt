# ── Web framework ─────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
python-multipart==0.0.9

# ── Rate limiting ─────────────────────────────────────────────────────────────
slowapi==0.1.9

# ── Job store & task queue ────────────────────────────────────────────────────
redis==5.0.4
celery==5.3.6          # replaces FastAPI BackgroundTasks for heavy pipeline work

# ── Forecasting (replaces XGBoost) ───────────────────────────────────────────
# CPU build by default — keeps the Docker image lean (~15GB vs ~45GB with
# CUDA wheels). The API/worker containers run CHRONOS_DEVICE=cpu (see
# .env.example). For local GPU development, use requirements-gpu.txt instead.
chronos-forecasting==1.3.0  # Amazon Chronos T5 — zero-shot time-series forecasting
torch==2.3.0                # CPU build (pulled from default PyPI index)

# ── Data ─────────────────────────────────────────────────────────────────────
numpy==1.26.4
pandas==2.2.2
openpyxl==3.1.2

# ── PDF generation (Auto Purchase Order Generator) ───────────────────────────
# >=4.0 required: reportlab's Paragraph engine handles Arabic bidi/shaping
# natively (verified against 4.4.10) as long as an Arabic-capable TTF font
# is available on the system — see reporting/purchase_order.py and the
# Dockerfile's `fonts-dejavu-core` / `fonts-freefont-ttf` packages.
reportlab>=4.0.0

# ── HTTP client ───────────────────────────────────────────────────────────────
requests==2.31.0

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
apscheduler==3.10.4     # scheduled monitoring jobs
